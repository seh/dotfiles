;:* personal.el
;:*=======================
;; If it's not initialized from the system:
(setq user-full-name "Steven E. Harris")
;:::::::::::::::::::::::::::::::::::::::::::::::::*
(message "personal settings initialized")
